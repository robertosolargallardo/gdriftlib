#include "Model.h" 

Model::Model(){
}

Model::~Model(){

}

void Model::run(Population *src, Population *dst, Pool *pool, Individual::Profile *profile){
	cerr<<"Model::run - Not Implemented.\n";
	exit(EXIT_FAILURE);
}
